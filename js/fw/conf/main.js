/**
 *
 *  The entrance of conf.
 *
 *  @author  Howard.Zuo
 *  @date    Feb 8th, 2015
 *
 **/
(function (define) {
    'use strict';

    define([
        './Router'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));