/**
 *
 *  The entrance of conf.
 *
 *  @author  Howard.Zuo
 *  @date    Jan 3th, 2015
 *
 **/
(function (define) {
    'use strict';

    define([
        './Router',
        './Globalization'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));