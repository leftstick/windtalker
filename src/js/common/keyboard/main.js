/**
 *
 *  The entrance of keyboard.
 *
 *  @author  Howard.Zuo
 *  @date    Jan 3th, 2015
 *
 **/
(function (define) {
    'use strict';

    define([
        './KeyBoard'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));