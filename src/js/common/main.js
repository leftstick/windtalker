/**
 *
 *  The entrance of common.
 *
 *  @author  Howard.Zuo
 *  @date    Jan 3th, 2015
 *
 **/
(function (define) {
    'use strict';

    define([
        './keyboard/main'
    ], function (keyboard) {
        var values = keyboard;
        return values;
    });

}(define));