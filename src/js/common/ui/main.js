/**
 *  Entrance of common ui
 *
 *
 *  @author  Hao.Zuo
 *  @date    Feb 11th, 2015
 *
 */
(function(define) {
    'use strict';

    define(['./alerts', './modal', './WindowResizer'], function() {
        var args = [].slice.call(arguments);
        return args;
    });

}(define));
