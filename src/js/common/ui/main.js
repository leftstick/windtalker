/**
 *  Entrance of common ui
 *
 *
 *  @author  Hao.Zuo
 *  @date    Feb 9th, 2015
 *
 */
(function(define) {
    'use strict';

    define(['./alerts', './WindowResizer'], function() {
        var args = [].slice.call(arguments);
        return args;
    });

}(define));
