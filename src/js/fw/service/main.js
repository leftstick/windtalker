/**
 *
 *  The entrance of utils.
 *
 *  @author  Howard.Zuo
 *  @date    Jan 3th, 2015
 *
 **/
(function (define) {
    'use strict';

    define([
        './utils'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));