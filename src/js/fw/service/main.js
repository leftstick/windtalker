/**
 *
 *  The entrance of utils.
 *
 *  @author  Howard.Zuo
 *  @date    Feb 9th, 2015
 *
 **/
(function(define) {
    'use strict';

    define([
        './events',
        './utils'
    ], function() {
        return [].slice.apply(arguments);
    });

}(define));
